pipeline {
    agent any
    tool { maven 'local maven'
    }
    stages{
        stage('Build'){
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'saving'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}
